#show outline: none

#show heading.where(level: 1): it => {
  set text(
    fill: rgb("#2F2E4E"),
    weight: "bold",
    size: 22pt
  )

  it
}

#show heading.where(level: 2): it => {
  set text(
    fill: rgb("#2F2E4E"),
    weight: "bold",
    size: 18pt
  )

  it
}